pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("io.micronaut.build.shared.settings") version "6.6.1"
}

rootProject.name = 'docs'

include "docs-asciidoc-config-props"

micronautBuild {
    useStandardizedProjectNames = true
    importMicronautCatalog()
}
